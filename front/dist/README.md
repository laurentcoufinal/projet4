# Assets statiques (servis à la racine)

- **Image de fond** : placer ici un fichier nommé `background.png` (ou `background.jpg` puis adapter le nom dans `src/styles/index.css`). L’image peut être exportée depuis la maquette Figma FileShare (fond « globe / réseau »).
